# Evidências P01

Execução em 23–24/09/2026 (America/Sao_Paulo). Windows, Python 3.13.15 via uv 0.12.18, Node 22.14.0, pnpm 10.34.5 e Docker Desktop. Dependências exatas nos lockfiles. Imagens Linux de API/web construídas e iniciadas com PostgreSQL real; nenhuma conexão ao MySQL.

## Resultados executados

| Verificação | Resultado |
|---|---|
| `python scripts/dev.py check` | Aprovado: Ruff/formatação, mypy, guard de arquitetura, scan inicial de arquivos/segredos, ESLint, TS, formatação web, contratos, build e suítes |
| pytest | 18 aprovados, incluindo banco real; zero integração pulada |
| Vitest / Testing Library | 4 aprovados: validação/foco/preservação de valor, teclado/feedback, falha/retry e loading |
| Playwright / Chromium | 6 aprovados: 3 cenários em desktop e Pixel 7 |
| axe nas páginas inicial/galeria | Zero violações nas tags WCAG 2 A/AA, 2.1 AA e 2.2 AA verificadas |
| Reflow | Sem overflow horizontal em 320px, desktop e celular nos testes |
| Build Vite | Aprovado; páginas carregadas sob demanda e duas fontes WOFF2 locais |
| Contratos | Export OpenAPI e tipos gerados correspondem à factory; somente live/ready expostos |
| Alembic/PostgreSQL | Banco vazio → head; upgrade repetido; downgrade → head; comparação metadata; revisão incompatível resulta 503 |
| Docker Compose | PostgreSQL/API/web saudáveis; migration job termina com êxito; DB runtime separado do migrator |
| Auditorias de dependências runtime | pnpm audit e pip-audit sem vulnerabilidades conhecidas reportadas na execução |
| Preservação | Main permanece no baseline; worktree original não alterado; tag publicada; documentos íntegros |
| CI GitHub Actions | Jobs `checks` e `browser` aprovados no commit de implementação, incluindo auditorias e instalação limpa em Linux |
| Clone limpo local | `init` e `up` aprovados em outra cópia sem .env, venv ou node_modules preexistentes; banco novo e serviços saudáveis |

O teste completo de navegador atravessa Vite → FastAPI → PostgreSQL. Uma rota de teste intercepta a chamada apenas no cenário explícito de falha de rede, remove a interceptação e confirma recuperação com backend real. Não há mocks permanentes no produto.

Os testes HTTP verificam mensagens genéricas, headers/correlation ID, omissão de entradas e de segredos em logs, erro 500, 404/405/422/503 e saúde independente do banco. O guard de arquitetura é testado com violações deliberadas, incluindo imports relativos.

## Revisão e reprodução

Commit de implementação: `a6912886b2155a56a93c12b4b053eef7f6751ea2`. [Execução do CI aprovada](https://github.com/O-marqs/petland-2.0/actions/runs/35990830904); [PR #1 em rascunho](https://github.com/O-marqs/petland-2.0/pull/1).

Em 24/09, foi criado um clone independente, sem hardlinks, em `C:\Users\LUCASMARQUESMARQUES\Documents\GitHub\petland-3.0-verificacao`. O projeto Compose `petland3-clean` usou volume PostgreSQL novo e segredos locais gerados pelo comando `init`. `python scripts/dev.py up` concluiu com código zero; PostgreSQL, API e web ficaram saudáveis. O endpoint de readiness retornou `{"status":"ready"}` e uma consulta ao banco confirmou `alembic_version = 0001_foundation`. O build local reutilizou camadas Docker em cache; o CI também executou o fluxo em runner Linux efêmero.

Os seis cenários Playwright foram executados novamente contra os serviços dessa cópia limpa, em desktop e celular: seis aprovados em 11,6 segundos. O executor de testes usou as dependências locais da cópia de desenvolvimento; a aplicação e o banco sob teste vieram do clone limpo. A [execução específica do PR](https://github.com/O-marqs/petland-2.0/actions/runs/35990861006) também concluiu com sucesso.

Evidências visuais inspecionadas: [página inicial desktop](screenshots/P01-home-desktop.png) e [galeria em celular](screenshots/P01-components-mobile.png). As capturas ilustram a P01; não demonstram funcionalidades comerciais posteriores.

## Correções descobertas durante a execução

Foram corrigidos: import de fontes incompatível com o pacote escolhido; imports explícitos de APIs do Vitest; tipagem de headers; sintaxe/formatação do verificador de arquitetura. O primeiro ensaio de navegador encontrou uma imagem Docker anterior à correção das fontes. Após reconstrução, os seis cenários passaram. Não foram relaxados critérios ou assertions para esconder esses problemas.

## Limitações

- A inspeção automatizada de acessibilidade não substitui leitor de tela, usabilidade ou auditoria completa.
- Não foram executados testes de concorrência comercial, autorização de usuários, sessão ou reserva: esses módulos ainda não existem e pertencem a fases futuras.
- O scan do estado ativo procura classes específicas de segredo; não certifica todo o histórico. A baseline contém os riscos documentados do legado.
- Aviso não bloqueante da dependência Starlette: suporte a httpx no TestClient está depreciado em favor de httpx2. Build também informa comentários de anotação removidos de Zod. Ambos permanecem visíveis; resultados foram aprovados com esses avisos.
